# Demo4 Real MUI

This demo verifies the skill with the actual Material UI library. It intentionally uses `@mui/material` primitives instead of static HTML imitation.

```bash
pnpm install
pnpm build
pnpm dev
```
